/* amalgame-formats-protobuf — stub runtime header.
 *
 * The codec is pure Amalgame (facade.am). The only C is two inline
 * @c{} bridges for UTF-8 string <-> byte-list conversion, which use
 * the AmalgameList + GC symbols already provided by the amc runtime,
 * so this header carries no declarations of its own. */
#ifndef AMALGAME_FORMATS_PROTOBUF_H
#define AMALGAME_FORMATS_PROTOBUF_H
#endif
